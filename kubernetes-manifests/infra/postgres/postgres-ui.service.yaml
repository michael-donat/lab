apiVersion: v1
kind: Service
metadata:
  name: postgres-ui
  namespace: infra
  labels:
    name: postgres-ui
  annotations:
    external-dns.alpha.kubernetes.io/hostname: postgres-ui.lab.donat.im
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: postgres-ui

